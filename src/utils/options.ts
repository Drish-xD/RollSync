// register form options
const classOptions = Array.from({ length: 12 }, (_, i) => i + 1);

const sectionOptions = Array.from({ length: 26 }, (_, index) => String.fromCharCode(65 + index));

export { classOptions, sectionOptions };
